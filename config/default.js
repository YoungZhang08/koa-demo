const config = {

    // 启动端口
    port: 8000,

    // 数据库配置
    database: {
        DATABASE: 'koa_demo',
        USERNAME: 'root',
        PASSWORD: 'zyy1001happiness',
        PORT: '3306',
        HOST: 'localhost'
    }
};

module.exports = config;